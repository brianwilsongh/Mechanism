<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wordpress.httpspandareaktor.mekanism">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:debuggable="false"
        android:screenOrientation="portrait"
        android:theme="@style/AppThemeNoActionBar">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PhysicsEquations"
            android:label="Physics Equations"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".KinematicsSolver1"
            android:theme="@style/AppThemeNoActionBar">
        </activity>
        <activity android:name=".KinematicsSolver2"
            android:theme="@style/AppThemeNoActionBar"></activity>
        <activity android:name=".ShowCalculation"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>